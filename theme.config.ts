import { COLORS } from "@/constants/colors";
import type { ThemeConfig } from "@/types/global.types";

/**
 * Configuration officielle du thème Kyrus Studio.
 */
export const themeConfig: ThemeConfig = {
  mode: "dark",
  primaryColor: COLORS.blueFlame,
  secondaryColor: COLORS.anthracite,
  accentColor: COLORS.brightLightBlue,
};
